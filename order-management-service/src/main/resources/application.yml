server:
  port: 8080
spring:
  application:
    name: order-management-service
  sleuth:
    sampler:
      probability: 1.0
  zipkin:
    base-url: http://localhost:9411
    service:
      name: ${spring.application.name}